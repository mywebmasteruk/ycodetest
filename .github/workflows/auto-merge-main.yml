name: Deprecated main-to-tenant sync

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Single-branch mode notice
        run: |
          echo "::notice::Single-branch mode enabled: main is the production branch."
          echo "::notice::This workflow is intentionally disabled."
