# Kept as a manual no-op so old workflow filenames still resolve in GitHub. Builder: **main** only.
name: Deprecated noop (historical)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Main-only policy
        run: |
          echo "::notice::Builder deploys from **main** only."
