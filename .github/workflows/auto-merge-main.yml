# Previously synced `main` → `tenant-multi`. Production now uses **main** only.
# Kept as a manual no-op so old workflow names still resolve; do not re-enable tenant-multi.
name: Deprecated — tenant-multi sync (noop)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Main-only policy
        run: |
          echo "::notice::masjidweb builder uses branch **main** only. No tenant-multi sync."
